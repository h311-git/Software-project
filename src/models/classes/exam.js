class exam {
  constructor(id, title, time, instructor, grade, questions, course) {
    this.id = id;
    this.title = title;
    this.time = time;
    this.instructor = instructor;
    this.grade = grade;
    this.questions = questions;
    this.course = course;
  }
}
